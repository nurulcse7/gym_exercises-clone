import React from 'react';
import { Stack } from '@mui/material';
// import { InfinitySpin } from 'react-loader-spinner';

const Loader = () => (
  <Stack direction="row" justifyContent="center" alignItems="center" width="100%">
    
  </Stack>
);

export default Loader;

// https://www.npmjs.com/package/react-loader-spinner // npm install react-loader-spinner --save 

// <InfinitySpin color="grey" /> // Not available

// TailSpin	<TailSpin color="#00BFFF" height={80} width={80} />  // available
